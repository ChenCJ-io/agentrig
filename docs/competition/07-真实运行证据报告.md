# 真实运行证据报告

> 证据冻结时间：2026-08-14（Asia/Shanghai）
>
> 公开主 Target：`target_editflow_http_sse`
>
> Driver：`http_sse`
>
> 被测模型：`deepseek-v4-flash`
> 状态：正式录制证据（干净 recording DB，全部 ID 见 `editflow-recording-evidence.json`）

## 结论

公开 EditFlow 主链已闭合：真实 Agno/DeepSeek 决策经过 AgentRig HTTP/SSE 进入受控工具结果链，
Before/Candidate 以 Prompt SHA、Case Snapshot 和 Manifest 证明可比性；真实 MCP 结果可由持久化事件生成
Draft Sample，经人工审核后低副作用回放；普通用户也能在 Web 助手中完成 Plan、确认、提交和 Run。

## Before / Candidate

| 字段 | Before | Candidate headline |
|---|---|---|
| Prompt SHA | `43157c294fef…9572` | `71adb390cb86…f7fd` |
| Run | `run_61e7649365274a238f566e40328d9c03` | `run_b8ac3aa9d5b1437ba0954144ff6024ea` |
| Manifest | `sha256:bb5008e6…66aa` | 同一 Manifest |
| Case | `case_editflow_one_step_mixed_chain` | 同一冻结 Case Snapshot |
| Cell / Attempt | 1 / 5 | 1 / 5 |
| 结果 | 2 Pass / 3 Behavior Fail | 5/5 Pass |

Before 失败的决定性事实是 `inspect_image` 有时出现在 `retouch_photo` 之后，违反人物保护前先检查的行为
合同。Candidate 修改只在 Prompt 和 `retouch_photo` 模型可见 description；进程重启后 SHA 变化，HTTP
适配层未新增硬编码路由。

## 最终回归矩阵

Run `run_3afacf3d9e304498b677265b11044793`，Manifest
`sha256:50adbfacf8ee62cd01fa38647f1f79f9575fc80b3e08442f30be3744f7cfbdaf`：

| Case | Attempts | 结果 | 核心合同 |
|---|---:|---:|---|
| `case_editflow_brighten_only` | 5 | 5/5 | 检查后调亮；无素材/裁剪过度路由 |
| `case_editflow_background` | 5 | 5/5 | 搜索素材后应用；人物保护 |
| `case_editflow_mixed_chain` | 5 | 5/5 | 专用工具依赖与图片引用串联 |
| `case_editflow_preserve_subject` | 5 | 5/5 | retouch/apply 均传播约束 |
| `case_editflow_crop_only` | 5 | 5/5 | 明确比例；允许可选只读检查 |
| `case_editflow_asset_miss` | 5 | 5/5 | 可有界改写查询；不编造 ID、不 apply |

总计 6 Cells / 30 Attempts，30/30 Pass，0 skipped，0 failure class。混合链的实际通过轨迹为：

```text
inspect_image ───────────────→ retouch_photo
search_assets ───────────────→ apply_asset → crop_photo
retouch result image-demo-edit-01 ─┘          │
apply result image-demo-edit-02 ──────────────┘
```

## Case 质量修复

第一轮扩展矩阵出现 `provider_exhausted`，事件显示模型做了合理 `inspect_image`，或空搜索后把“极光沙漠”
改为“沙漠”再查一次。它们被归类为 `CASE_INVALID` 而非 behavior fail：增加可选 inspect Fixture，并把
空搜索 Fixture 设为 repeatable。修正后 `asset_miss` 单独 5/5，最终矩阵 30/30。

## Real Tool → Sample → Replay

| 字段 | 实测值 |
|---|---|
| Capture Run | `run_db173d3e5b314568bfad82d2b3fc59ef` |
| Real backend | `editflow__inspect_image` |
| Source event | `evt_32fb37661a564843b5973c60ad7ff80d` |
| Sample | `sample_934464c5a0414796839cac6d821b1339` |
| Sample provenance | `source_type=real_tool`；人工批准为 approved |
| Replay Run | `run_1a209c84b61f4390bf468af8865f0b90` |
| Replay | 5/5 Pass；5 Sample hit；0 real_tool Provider Attempt |

Sample 只覆盖 `inspect_image`，不声称采集了完整五工具链；“零真实调用”只限定在该 Replay Run。

## Codex CLI 确认批次

`codex exec` 真实读取项目 Skill 后,经 agentrig MCP 提交 `run_79367abb0d5b4db2b743e143d130cb99`
(同一冻结 Manifest,5/5 Pass,每次 8/8 断言),并按 Skill 对"只跑 headline"的会话范围如实输出
PARTIAL。终端实拍:`assets/live/codex-01-skill-session.png`、`codex-02-regression-verdict.png`。

## Web Assistant

- Session：`asst_5740815c6ef248d2992f77c6fc8d970e`；
- Plan：`plan_a0d80e75e2634b5f81fe751a197bbdbb`；
- 自然语言指定已有 Case、Target、Profile 与重复 3 次；
- `repeated execution` 触发确认；确认后尚未创建 Run；提交后创建 Run；
- Run：`run_a1c88bc8ed83474a9261df9e1d0d23fb`，3/3 Pass；
- Assistant 终态明确 `completed` 仍需依据 Cell/Attempt 总结。

## Browser 与仓库验收

- EditFlow：Ruff pass；34 non-live tests passed，1 live deselected；
- AgentRig Web：typecheck pass；真实后端 `editflow-recording.spec.ts` 1 passed；
- Axe：critical/serious 0；
- 新截图：`docs/competition/assets/live/editflow-01-overview.png` 至 `editflow-05-assistant.png`；
- 助手滚动消息区已补 `role=log`、可聚焦与 focus-visible，修复 Safari 键盘访问问题。

## 真实生产形态扩展证据

此前 lassist 验收仍保留为附录：三个真实语义场景共 9/9 Pass、一个参数诊断 1/1 Expected Fail，以及
Codex MCP 1/1 Pass。它证明 AgentRig 不只适配 EditFlow，但因脱敏要求不进入公开主视频。

## 诚实边界

- 彩排数据库和 ID 不等于正式录制证据；本报告全部 ID 来自 2026-08-14 正式录制库；
- fixture 替代工具副作用，不替代模型决策；
- `completed` 是生命周期状态，不自动等于 `pass`；
- Rule 的精确引用断言使用稳定 Fixture；动态 Real MCP 串联以 Timeline 证明；
- AgentScope/AgentTeams 外部 Live、Kubernetes 与目标容量需独立验收。
