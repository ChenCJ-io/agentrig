# AgentTeams v1.1.2 competition baseline

This profile is a read-only compatibility pointer to the original competition assets:

- `../../resources-v1.1.2.yaml`
- `../../VERSION`
- `../../dist/agentrig-{manager,curator,judge}.zip`

Do not regenerate or overwrite historical evidence with the v1.2.2 adapter. New development
uses the sibling `v1.2.2-current` profile explicitly.
