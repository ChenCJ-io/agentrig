## 动机 / Motivation

为什么改？（链接相关 issue）

## 改动 / Changes

做了什么？

## 验证 / Verification

- [ ] `uv run ruff check` 过
- [ ] `uv run mypy` 过
- [ ] `uv run pytest` 过（或补充了新测试）
- [ ] 若改 `web/`：`cd web && npm run build` 过

## 关联 issue

Closes #
